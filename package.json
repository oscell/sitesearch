{
  "name": "@algolia/sitesearch-root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "bun --filter @algolia/registry dev",
    "dev:docs": "bun --filter docs dev",
    "build": "bun --filter @algolia/registry registry:build && bun --filter @algolia/registry build",
    "build:js": "bun --filter @algolia/sitesearch-js build",
    "build:docs": "bun --filter docs build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.1.29",
  "devDependencies": {
    "@biomejs/biome": "2.0.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.0.0"
  }
}
